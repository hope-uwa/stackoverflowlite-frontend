version: '2'
checks:
  file-lines:
    enabled: false
  similar-code:
    enabled: false
  method-length:
    enabled: false
plugins: 
  eslint:
    enabled: true
    channel: "eslint-5"
    config:
      config: .eslintrc.json
      extensions:
        - .es6
        - .js
